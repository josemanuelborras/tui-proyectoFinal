from django.urls import path
from . import views

urlpatterns = [
    # path('materias/', views.materias_list, name='materias_list'),
    # path('materias/crear/', views.materia_create, name='materia_create'),
    # path('eliminar/<int:materia_id>/', views.materia_delete, name='materia_delete'),
    # path('editar/<int:materia_id>/', views.materia_edit, name='materia_edit'),
]    
